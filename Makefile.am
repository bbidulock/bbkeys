# not a GNU package. You can remove this line, if 
# have all needed files, that a GNU package needs
AUTOMAKE_OPTIONS = foreign 

SUBDIRS = src data doc

docdir = $(prefix)/doc/bbkeys
doc_DATA = README AUTHORS ChangeLog
EXTRA_DIST = $(doc_DATA)
