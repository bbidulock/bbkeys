- perl configurator script needs to be rewritten for the new file syntax 
  (must handle config options as well as keybindings)

- popup window-cycling menu/list needs to be implemented still

- other stuff I'm sure
